{
    "name": "@paulborm/gh-actions-playground",
    "description": "",
    "scripts": {
        "build": "mkdir dist && cp -r src dist && echo \"Building something ...\"",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "repository": {
        "type": "git",
        "url": "https://github.com/paulborm/gh-actions-playground.git"
    }
}
